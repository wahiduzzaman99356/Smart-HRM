# HRIS

## Workflow Note
- For any module change, API spec updates are required.
- API spec edits must be confirmed by the requester first (ask before updating).
- Keep all API spec markdown files in `docs/`.
